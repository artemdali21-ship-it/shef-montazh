## Компонент StarRating - Интеграция и использование

### 📦 Созданные файлы:
- `components/rating/StarRating.tsx` - основной компонент
- `components/rating/index.ts` - экспорты для удобного импорта

### 🎨 Интегрированы в:
1. **ShiftCard** (`components/features/shift-card.tsx`)
   - Отображает рейтинг заказчика ниже его имени
   - Размер: `sm` (маленькие звезды)
   - Показывает рейтинг и количество отзывов
   - Пример: "4.5 (12)"

2. **UserCard** (`components/features/user-card.tsx`)
   - Отображает рейтинг рабочего в заголовке
   - Размер: `md` (средние звезды)
   - Показывает рейтинг и количество отзывов
   - Пример: "4.8 (23)"

### 💻 Использование компонента:

```tsx
import { StarRating } from '@/components/rating';

// Базовое использование
<StarRating rating={4.5} />

// С количеством отзывов
<StarRating rating={4.8} reviewCount={23} size="md" />

// Маленький размер без количества отзывов
<StarRating rating={3.2} size="sm" showNumber={false} />

// Большой размер
<StarRating rating={5} reviewCount={10} size="lg" showNumber={true} />
```

### 🎯 Props компонента StarRating:
- `rating` (number, обязательно) - рейтинг от 0 до 5 с поддержкой дробных значений
- `reviewCount` (number, опционально) - количество отзывов
- `size` (sm | md | lg) - размер звёзд, по умолчанию "md"
- `showNumber` (boolean) - показывать ли рейтинг число, по умолчанию true

### 🎨 Цветовая схема:
- **Заполненные звёзды**: `#E85D2F` (оранжевый, основной цвет приложения)
- **Пустые звёзды**: `#666666` (серый)
- **Поддержка половинок**: например, 4.5 звёзд отобразит 4 полные + 1 половинка

### 📱 Размеры:
- `sm`: w-3 h-3 (маленький текст, для карточек смен)
- `md`: w-4 h-4 (средний текст, для карточек рабочих)
- `lg`: w-5 h-5 (большой текст, для профилей)

### ✨ Особенности:
- Полностью типизирован TypeScript
- Поддержка половинок звёзд (4.5 показывает как 4 полные + половинка)
- Smooth анимация масштабирования при наведении
- Оптимизирован с React.memo для производительности
- Использует lucide-react для иконок
- Стили через Tailwind CSS

### 📚 Примеры в коде:

**ShiftCard с рейтингом заказчика:**
```tsx
<ShiftCard
  title="Монтаж вентиляции"
  client="Decor Factory"
  clientRating={4.5}
  clientReviewCount={12}
  location="Москва, ЦАО"
  // ... остальные props
/>
```

**UserCard с рейтингом рабочего:**
```tsx
<UserCard
  name="Иван Петров"
  rating={4.8}
  reviewCount={23}
  completedShifts={15}
  // ... остальные props
/>
```

### 🔄 Миграция старого кода:
Старый компонент `RatingStars` использовал жёлтый цвет (#FFD60A) и остался без изменений.
Новый `StarRating` использует оранжевый (#E85D2F) и предоставляет удобное отображение количества отзывов.
