**ТОЧНО\! 🎯**

В `05_BACKLOG.md` есть задачи, но это просто список приоритетов. А тебе нужен **БОЕВОЙ ПЛАН** — что делать каждый день, в какой последовательности, с чеклистами\!

Создам **`07_ACTION_PLAN_3WEEKS.md`** — это будет твоя **дорожная карта** на 21 день.

---

## **📋 Что будет в файле:**

1. **День за днём** — конкретные задачи на каждый день  
2. **Чеклисты** — что должно быть готово к концу дня  
3. **Зависимости** — что нужно сделать перед чем  
4. **Оценки времени** — сколько часов на задачу  
5. **Definition of Done** — критерии готовности  
6. **Ссылки на файлы** — где писать код  
7. **Тестовые сценарии** — как проверить что работает  
8. **Milestones** — контрольные точки (конец недели)

---

Создаю файл прямо сейчас\! 🚀

cd /home/claude && cat \> 07\_ACTION\_PLAN\_3WEEKS.md \<\< 'EOF'  
\# 🚀 ACTION PLAN: 3 НЕДЕЛИ ДО ПОЛНОЦЕННОГО ПРОДУКТА

\> \*\*Цель:\*\* Создать production-ready продукт-кейс, который можно показать инвесторам и запустить пилот с реальными клиентами.  
\>   
\> \*\*Старт:\*\* После завершения базового MVP (check-in, рейтинги, категории)  
\>   
\> \*\*Финиш:\*\* Полнофункциональная платформа с оплатой, чатом, верификацией, админкой

\---

\#\# 📊 OVERVIEW

\#\#\# Что будет готово через 3 недели:

\*\*Для Workers:\*\*  
\- ✅ Онбординг с выбором категорий  
\- ✅ Портфолио с фото работ  
\- ✅ Поиск смен с продвинутыми фильтрами  
\- ✅ Check-in с фото \+ геолокация  
\- ✅ Чат с заказчиком  
\- ✅ Рейтинги и бейджи достижений  
\- ✅ История заработка \+ графики  
\- ✅ Верификация профиля (SMS)

\*\*Для Clients:\*\*  
\- ✅ Wizard создания смены (6 шагов)  
\- ✅ Просмотр откликов  
\- ✅ Избранные исполнители \+ блокировка  
\- ✅ Чат с исполнителем  
\- ✅ Оплата через ЮКасса  
\- ✅ История платежей \+ статистика  
\- ✅ PDF акты  
\- ✅ Рейтинги исполнителей

\*\*Админ-панель:\*\*  
\- ✅ Управление пользователями (бан/разбан)  
\- ✅ Модерация смен  
\- ✅ Базовая аналитика  
\- ✅ Финансовые отчёты

\---

\#\# 🗓️ НЕДЕЛЯ 1: ЯДРО \+ ДЕНЬГИ

\*\*Цель недели:\*\* Завершить критичную функциональность — оплата, поиск, уведомления, доверие

\---

\#\#\# 📅 ДЕНЬ 1: ЮКасса — Создание платежа

\*\*Главная цель:\*\* Зарегистрироваться в ЮКасса и реализовать создание платежа

\*\*Задачи:\*\*

1\. \*\*Регистрация в ЮКасса (1 час)\*\*  
   \- Зарегистрировать магазин на \[yookassa.ru\](https://yookassa.ru)  
   \- Получить \`shop\_id\` и \`secret\_key\` для test-режима  
   \- Добавить в \`.env.local\`:  
     \`\`\`  
     YUKASSA\_SHOP\_ID=your\_shop\_id  
     YUKASSA\_SECRET\_KEY=your\_secret\_key  
     \`\`\`

2\. \*\*Установить SDK (15 мин)\*\*  
   \`\`\`bash  
   npm install @a2seven/yoo-checkout

3. **Создать API endpoint `/api/payments/create` (2 часа)**

   * Принимает: `shift_id`, `amount`, `client_id`, `worker_id`  
   * Создаёт платёж в ЮКасса  
   * Сохраняет в таблицу `payments`:  
     * `yukassa_payment_id`  
     * `status = 'pending'`  
     * `amount`, `platform_fee = 1200`  
   * Возвращает: `confirmation_url` для оплаты  
4. **Создать компонент `PaymentButton` (1 час)**

   * Файл: `components/payments/PaymentButton.tsx`  
   * Кнопка "Оплатить {amount}₽"  
   * При клике: вызов API → редирект на ЮКасса  
   * Loading state  
5. **Интегрировать на страницу смены (30 мин)**

   * Файл: `app/(client)/shift/[id]/page.tsx`  
   * Показывать кнопку если `shift.status === 'completed'` и `payment.status === 'pending'`

**Чеклист готовности:**

* \[ \] Зарегистрирован в ЮКасса, есть test-ключи  
* \[ \] API endpoint `/api/payments/create` работает  
* \[ \] Можно создать платёж и получить `confirmation_url`  
* \[ \] Кнопка оплаты на странице смены  
* \[ \] При клике открывается форма ЮКасса

**Файлы для работы:**

* `app/api/payments/create/route.ts` — новый  
* `components/payments/PaymentButton.tsx` — новый  
* `app/(client)/shift/[id]/page.tsx` — обновить  
* `lib/yukassa.ts` — новый (helper функции)

**Тестовый сценарий:**

1. Создать смену → завершить её  
2. Кликнуть "Оплатить"  
3. Должен открыться виджет ЮКасса  
4. Провести тестовую оплату (тестовая карта: 5555 5555 5555 4477\)

---

### **📅 ДЕНЬ 2: ЮКасса — Webhook \+ статусы**

**Главная цель:** Обработка подтверждения оплаты и обновление статусов

**Задачи:**

1. **Создать webhook endpoint `/api/webhooks/yukassa` (2 часа)**

   * Принимает POST от ЮКасса  
   * Проверяет подпись (безопасность)  
   * Обрабатывает события:  
     * `payment.succeeded` → обновить `payments.status = 'paid'`, `paid_at = now()`  
     * `payment.canceled` → `status = 'failed'`  
   * Обновить статус смены: `shifts.status = 'payout'`  
   * Создать уведомление для worker: "Оплата получена"  
2. **Настроить webhook в ЮКасса (15 мин)**

   * В личном кабинете ЮКасса указать URL: `https://your-domain.vercel.app/api/webhooks/yukassa`  
   * Тип: HTTP метод POST  
3. **Создать компонент `PaymentStatus` (1 час)**

   * Файл: `components/payments/PaymentStatus.tsx`  
   * Badges:  
     * "Ожидает оплаты" (оранжевый)  
     * "Оплачено" (зелёный)  
     * "Ошибка оплаты" (красный)  
   * Иконки из lucide-react  
4. **Страница истории платежей (2 часа)**

   * Для client: `app/(client)/payments/page.tsx`  
   * Для worker: `app/(worker)/payments/page.tsx`  
   * Таблица платежей:  
     * Смена (название)  
     * Дата  
     * Сумма  
     * Статус  
   * Фильтр по статусу  
   * Пагинация  
5. **Тестирование полного флоу (1 час)**

   * От создания смены до получения оплаты  
   * Проверить webhook локально через ngrok

**Чеклист готовности:**

* \[ \] Webhook обрабатывает события от ЮКасса  
* \[ \] После успешной оплаты статус меняется на 'paid'  
* \[ \] Worker получает уведомление  
* \[ \] Страница истории платежей работает  
* \[ \] Тестовая оплата проходит end-to-end

**Файлы для работы:**

* `app/api/webhooks/yukassa/route.ts` — новый  
* `components/payments/PaymentStatus.tsx` — новый  
* `app/(client)/payments/page.tsx` — новый  
* `app/(worker)/payments/page.tsx` — новый

**Тестовый сценарий:**

1. Создать смену → завершить → оплатить  
2. Дождаться webhook от ЮКасса (\~3-5 сек)  
3. Статус должен измениться на "Оплачено"  
4. Worker должен увидеть уведомление

---

### **📅 ДЕНЬ 3: Поиск смен — Фильтры \+ Сортировка**

**Главная цель:** Продвинутая система поиска смен для workers

**Задачи:**

1. **Обновить API `/api/shifts/search` (2 часа)**

   * Файл: `app/api/shifts/search/route.ts`  
   * Параметры:  
     * `category` (array)  
     * `date_from`, `date_to`  
     * `district` (Москва, список районов)  
     * `pay_min`, `pay_max`  
     * `client_rating_min`  
     * `sort_by` (date/pay/rating)  
     * `sort_order` (asc/desc)  
   * Использовать Supabase query builder для фильтрации  
   * Добавить pagination (`page`, `limit`)  
2. **Создать компонент `ShiftFilters` (2 часа)**

   * Файл: `components/search/ShiftFilters.tsx`  
   * Collapsible секция фильтров  
   * Чекбоксы категорий  
   * Date range picker (react-day-picker)  
   * Select районов Москвы  
   * Range slider цены (5k \- 100k)  
   * Slider рейтинга (1-5)  
   * Кнопка "Применить фильтры"  
   * Кнопка "Сбросить"  
3. **Создать компонент `ShiftSort` (30 мин)**

   * Dropdown с опциями:  
     * По дате (новые/старые)  
     * По цене (высокая/низкая)  
     * По рейтингу заказчика  
4. **Обновить страницу `/search` (worker) (2 часа)**

   * Файл: `app/(worker)/search/page.tsx`  
   * Интегрировать `ShiftFilters` и `ShiftSort`  
   * Сохранять фильтры в URL query params  
   * Loading skeleton пока грузятся данные  
   * Показывать кол-во найденных смен  
   * Пагинация  
5. **Сохранение фильтров в localStorage (30 мин)**

   * При выборе фильтров → сохранять в localStorage  
   * При возврате на страницу → восстанавливать

**Чеклист готовности:**

* \[ \] API принимает все фильтры и возвращает отфильтрованные смены  
* \[ \] Фильтры работают (категории, дата, район, цена, рейтинг)  
* \[ \] Сортировка работает  
* \[ \] Результаты отображаются корректно  
* \[ \] Фильтры сохраняются при перезагрузке

**Файлы для работы:**

* `app/api/shifts/search/route.ts` — обновить  
* `components/search/ShiftFilters.tsx` — новый  
* `components/search/ShiftSort.tsx` — новый  
* `app/(worker)/search/page.tsx` — обновить

**Тестовый сценарий:**

1. Открыть `/search`  
2. Выбрать категорию "Монтажник"  
3. Установить цену от 10k до 30k  
4. Выбрать район "ЦАО"  
5. Сортировать по цене (высокая → низкая)  
6. Результаты должны соответствовать фильтрам

---

### **📅 ДЕНЬ 4: Push-уведомления через Telegram**

**Главная цель:** Настроить реал-тайм уведомления для всех событий

**Задачи:**

1. **Создать Telegram бота (30 мин)**

   * Зарегистрировать бота через [@BotFather](https://t.me/botfather)  
   * Получить `BOT_TOKEN`

Добавить в `.env.local`:  
 TELEGRAM\_BOT\_TOKEN=your\_bot\_token

*   
2. **Создать `/api/telegram/send` helper (1 час)**

   * Файл: `lib/telegram.ts`  
   * Функция `sendTelegramNotification(telegram_id, message, type)`

Использовать Telegram Bot API:  
 POST https://api.telegram.org/bot{token}/sendMessage

*   
  * Форматирование: HTML markup  
3. **Создать notification triggers (3 часа)**

   * Файл: `lib/notifications.ts`  
   * 7 типов уведомлений:

// 1\. Новая смена в районе  
notifyNewShiftInArea(worker\_ids, shift)

// 2\. Отклик одобрен  
notifyApplicationApproved(worker\_id, shift)

// 3\. Смена скоро начнётся (1 час до)  
notifyShiftStartingSoon(worker\_id, shift)

// 4\. Исполнитель вышел на объект  
notifyWorkerCheckedIn(client\_id, worker, shift)

// 5\. Смена завершена  
notifyShiftCompleted(client\_id, worker\_id, shift)

// 6\. Платёж получен  
notifyPaymentReceived(worker\_id, payment)

// 7\. Просьба оценить  
notifyRatingRequest(user\_id, shift)

4.   
5. **Интегрировать triggers в ключевые точки (2 часа)**

   * В `/api/applications/[id]/approve` → уведомление worker  
   * В `/api/shift-workers/checkin` → уведомление client  
   * В `/api/shifts/[id]/complete` → уведомления обеим сторонам  
   * В webhook ЮКасса → уведомление worker о платеже  
6. **Создать страницу настроек уведомлений (1 час)**

   * Файл: `app/(shared)/settings/notifications/page.tsx`  
   * Toggle switches для каждого типа уведомлений  
   * Сохранять в `user_preferences` (JSONB)

**Чеклист готовности:**

* \[ \] Telegram бот создан, токен работает  
* \[ \] Можно отправить тестовое сообщение  
* \[ \] Все 7 типов уведомлений работают  
* \[ \] Уведомления приходят в правильные моменты  
* \[ \] Можно отключить типы уведомлений в настройках

**Файлы для работы:**

* `lib/telegram.ts` — новый  
* `lib/notifications.ts` — новый  
* `app/api/telegram/send/route.ts` — новый (опционально)  
* `app/(shared)/settings/notifications/page.tsx` — новый

**Тестовый сценарий:**

1. Одобрить отклик на смену  
2. Worker должен получить Telegram-сообщение  
3. Сделать check-in  
4. Client должен получить уведомление с фото  
5. Оплатить смену  
6. Worker должен получить уведомление о платеже

---

### **📅 ДЕНЬ 5: Избранное \+ Блокировка**

**Главная цель:** White/Black списки для формирования проверенной базы

**Задачи:**

1. **Создать API endpoints (1 час)**

   * `/api/favorites/add` — POST  
   * `/api/favorites/remove` — DELETE  
   * `/api/favorites/list` — GET  
   * `/api/blocked/add` — POST (с причиной)  
   * `/api/blocked/remove` — DELETE  
   * `/api/blocked/list` — GET  
2. **Добавить кнопки на профили (1 час)**

   * Компонент: `components/user/FavoriteButton.tsx`  
   * Иконка ⭐ (пустая/заполненная)  
   * Компонент: `components/user/BlockButton.tsx`  
   * Иконка 🚫  
   * Modal с выбором причины блокировки  
3. **Создать страницу избранных (2 часа)**

   * Файл: `app/(client)/favorites/page.tsx`  
   * Grid карточек избранных workers  
   * Фильтры: категория, рейтинг, доступность  
   * Кнопка "Быстрый найм" на каждой карточке  
4. **Создать страницу заблокированных (1 час)**

   * Файл: `app/(client)/blocked/page.tsx`  
   * Список заблокированных с причинами  
   * Кнопка "Разблокировать"  
5. **Интегрировать фильтр "Только избранные" в поиск (1 час)**

   * Добавить toggle в `ShiftFilters`  
   * API должен фильтровать по `favorites` таблице  
6. **Скрывать заблокированных пользователей (30 мин)**

   * В поиске workers не показывать заблокированных  
   * В откликах не показывать заблокированных

**Чеклист готовности:**

* \[ \] Можно добавить worker в избранное  
* \[ \] Можно заблокировать worker с указанием причины  
* \[ \] Страница `/favorites` показывает список  
* \[ \] Фильтр "Только избранные" работает в поиске  
* \[ \] Заблокированные не видны в результатах

**Файлы для работы:**

* `app/api/favorites/*` — новые  
* `app/api/blocked/*` — новые  
* `components/user/FavoriteButton.tsx` — новый  
* `components/user/BlockButton.tsx` — новый  
* `app/(client)/favorites/page.tsx` — новый  
* `app/(client)/blocked/page.tsx` — новый

**Тестовый сценарий:**

1. Открыть профиль worker  
2. Кликнуть ⭐ → добавлен в избранное  
3. Открыть `/favorites` → worker там есть  
4. Кликнуть 🚫 → указать причину → заблокировать  
5. Worker больше не видно в поиске

---

### **📅 ДЕНЬ 6-7: История платежей \+ Базовая статистика**

**Главная цель:** Финансовая прозрачность и первые графики

**Задачи День 6:**

1. **Расширить страницу истории платежей (2 часа)**

   * Для worker: `app/(worker)/payments/page.tsx`  
     * Сумма заработано за всё время  
     * Ожидает оплаты (pending)  
     * Средний чек за смену  
     * Последние 20 платежей (таблица)  
   * Для client: `app/(client)/payments/page.tsx`  
     * Потрачено за всё время  
     * Активных выплат (pending)  
     * Средняя стоимость смены  
     * Последние 20 платежей  
2. **Фильтры в истории (1 час)**

   * По статусу (все/оплачено/ожидает/ошибка)  
   * По дате (date range picker)  
   * По сумме (от-до)  
3. **Экспорт в CSV (1 час)**

   * Кнопка "Экспорт в Excel"  
   * Генерация CSV файла  
   * Скачивание  
4. **Детальная страница платежа (1 час)**

   * `app/payments/[id]/page.tsx`  
   * Вся информация о платеже:  
     * ID транзакции ЮКассы  
     * Дата создания / оплаты  
     * Смена (ссылка)  
     * Сумма, комиссия  
     * Метод оплаты  
     * Чек (если есть)

**Задачи День 7:**

5. **Создать страницу статистики для worker (3 часа)**

   * Файл: `app/(worker)/stats/page.tsx`  
   * Карточки метрик:  
     * Всего смен отработано  
     * Заработано (₽)  
     * Средний рейтинг  
     * % успешных смен  
   * График заработка по месяцам (recharts line chart)  
   * График смен по категориям (pie chart)  
6. **Создать страницу статистики для client (3 часа)**

   * Файл: `app/(client)/stats/page.tsx`  
   * Карточки метрик:  
     * Опубликовано смен  
     * Потрачено (₽)  
     * Средняя оценка исполнителей  
     * % завершённых без проблем  
   * График расходов по месяцам  
   * ТОП-5 исполнителей (по рейтингу \+ кол-ву смен)

**Установить библиотеку графиков (15 мин)**

 npm install recharts

7. 

**Чеклист готовности:**

* \[ \] История платежей с фильтрами  
* \[ \] Можно экспортировать в CSV  
* \[ \] Детальная страница платежа работает  
* \[ \] Страница статистики worker с графиками  
* \[ \] Страница статистики client с ТОП-5  
* \[ \] Графики отображаются корректно

**Файлы для работы:**

* `app/(worker)/payments/page.tsx` — обновить  
* `app/(client)/payments/page.tsx` — обновить  
* `app/payments/[id]/page.tsx` — новый  
* `app/(worker)/stats/page.tsx` — новый  
* `app/(client)/stats/page.tsx` — новый  
* `components/charts/EarningsChart.tsx` — новый  
* `components/charts/ExpensesChart.tsx` — новый

**Тестовый сценарий:**

1. Завершить несколько смен  
2. Оплатить их  
3. Открыть `/stats` как worker  
4. График должен показать заработок  
5. Открыть `/stats` как client  
6. Должен быть ТОП-5 исполнителей

---

### **🎯 MILESTONE: КОНЕЦ НЕДЕЛИ 1**

**Проверочный чеклист:**

* \[ \] ✅ Можно оплатить смену через ЮКасса  
* \[ \] ✅ Webhook обрабатывает подтверждение оплаты  
* \[ \] ✅ История платежей работает  
* \[ \] ✅ Поиск смен с фильтрами (категория, дата, район, цена)  
* \[ \] ✅ Сортировка смен работает  
* \[ \] ✅ Push-уведомления приходят в Telegram (7 типов)  
* \[ \] ✅ Можно добавить в избранное / заблокировать  
* \[ \] ✅ Статистика с графиками (worker \+ client)

**Что должно работать end-to-end:**

1. Worker находит смену через поиск  
2. Откликается  
3. Client одобряет → worker получает push  
4. Worker делает check-in → client получает push с фото  
5. Завершают смену → оба получают push  
6. Оценивают друг друга  
7. Client оплачивает через ЮКасса  
8. Worker получает push о платеже  
9. Оба видят историю и статистику

---

## **🗓️ НЕДЕЛЯ 2: ВКУСНЯШКИ \+ ПОЛИРОВКА**

**Цель недели:** Добавить премиальные фичи — чат, онбординг, портфолио, wizard

---

### **📅 ДЕНЬ 8-9: Чат 1-на-1 (Realtime)**

**Главная цель:** Полноценная переписка между заказчиком и исполнителем

**Задачи День 8:**

**Создать таблицу `messages` в Supabase (30 мин)**

 CREATE TABLE messages (  
  id UUID PRIMARY KEY DEFAULT uuid\_generate\_v4(),  
  from\_user\_id UUID REFERENCES users(id),  
  to\_user\_id UUID REFERENCES users(id),  
  shift\_id UUID REFERENCES shifts(id),  
  content TEXT NOT NULL,  
  image\_url TEXT,  
  is\_read BOOLEAN DEFAULT false,  
  created\_at TIMESTAMP DEFAULT NOW()  
);

CREATE INDEX idx\_messages\_users ON messages(from\_user\_id, to\_user\_id);  
CREATE INDEX idx\_messages\_shift ON messages(shift\_id);

1.   
2. **Настроить Supabase Realtime (30 мин)**

   * В Supabase Dashboard → Database → Replication  
   * Включить Realtime для таблицы `messages`  
3. **Создать API endpoints (1 час)**

   * `/api/messages/send` — POST (отправка сообщения)  
   * `/api/messages/list` — GET (список чатов)  
   * `/api/messages/conversation` — GET (переписка с конкретным юзером)  
   * `/api/messages/mark-read` — POST (отметить как прочитано)  
4. **Создать компонент `ChatList` (2 часа)**

   * Файл: `components/chat/ChatList.tsx`  
   * Список всех чатов (последнее сообщение, дата)  
   * Badge с кол-вом непрочитанных  
   * Аватар собеседника  
   * Клик → открыть переписку

**Задачи День 9:**

5. **Создать компонент `ChatWindow` (3 часа)**

   * Файл: `components/chat/ChatWindow.tsx`  
   * Полноэкранное окно чата  
   * Header: аватар \+ имя собеседника  
   * Messages list (scroll to bottom)  
   * Input для текста  
   * Кнопка отправки фото  
   * Loading state при отправке

**Настроить Realtime subscription (2 часа)**

 const subscription \= supabase  
  .channel('messages')  
  .on('postgres\_changes', {  
    event: 'INSERT',  
    schema: 'public',  
    table: 'messages',  
    filter: \`to\_user\_id=eq.${userId}\`  
  }, (payload) \=\> {  
    // Новое сообщение → добавить в список  
  })  
  .subscribe()

6.   
7. **Создать страницы чата (1 час)**

   * `app/(shared)/messages/page.tsx` — список чатов  
   * `app/(shared)/messages/[userId]/page.tsx` — переписка  
8. **Уведомления о новых сообщениях (30 мин)**

   * Badge на иконке Messages в навигации  
   * Push в Telegram при получении сообщения

**Чеклист готовности:**

* \[ \] Таблица messages создана, Realtime включён  
* \[ \] Можно отправить текстовое сообщение  
* \[ \] Можно отправить фото  
* \[ \] Сообщения приходят в реальном времени (без перезагрузки)  
* \[ \] Список чатов с непрочитанными  
* \[ \] Push-уведомление при новом сообщении

**Файлы для работы:**

* `app/api/messages/*` — новые  
* `components/chat/ChatList.tsx` — новый  
* `components/chat/ChatWindow.tsx` — новый  
* `app/(shared)/messages/page.tsx` — новый  
* `app/(shared)/messages/[userId]/page.tsx` — новый

**Тестовый сценарий:**

1. Client отправляет сообщение worker  
2. Worker открывает чат — сообщение там без перезагрузки  
3. Worker отвечает  
4. Client видит ответ в реальном времени  
5. Отправить фото → фото отображается

---

### **📅 ДЕНЬ 10: Онбординг**

**Главная цель:** Первое впечатление WOW — пошаговое знакомство с платформой

**Задачи:**

1. **Создать страницу приветствия (1 час)**

   * Файл: `app/onboarding/welcome/page.tsx`  
   * Логотип \+ анимация  
   * Заголовок: "Добро пожаловать в Шеф-Монтаж"  
   * Краткое описание (1-2 предложения)  
   * Кнопка "Начать"  
2. **Создать страницу выбора роли (1 час)**

   * Файл: `app/onboarding/role/page.tsx`  
   * 3 карточки:  
     * 👷 Я исполнитель (Worker)  
     * 🏢 Я заказчик (Client)  
     * 👨‍💼 Я шеф-монтажник (Shef)  
   * При клике → сохранить роль в `users.role`  
3. **Создать step-by-step заполнение профиля (3 часа)**

   * Файл: `app/onboarding/profile/page.tsx`  
   * Для Worker:  
     * Шаг 1: ФИО, телефон, город  
     * Шаг 2: Выбор категорий (чекбоксы)  
     * Шаг 3: Опыт работы (годы)  
     * Шаг 4: Фото профиля  
   * Для Client:  
     * Шаг 1: Название компании, ИНН  
     * Шаг 2: Контактное лицо, телефон  
     * Шаг 3: Описание деятельности  
   * Progress bar вверху (1/4, 2/4...)  
   * Кнопки "Назад" / "Далее"  
4. **Создать финальный экран (30 мин)**

   * Файл: `app/onboarding/complete/page.tsx`  
   * "Готово\! 🎉"  
   * Кнопка "Начать работу" → редирект на dashboard  
5. **Логика показа онбординга (30 мин)**

   * Проверять флаг `users.onboarding_completed`  
   * Если `false` → редирект на `/onboarding/welcome`  
   * После завершения → `onboarding_completed = true`  
6. **Анимации (1 час)**

   * Установить `framer-motion`  
   * Плавные переходы между шагами  
   * Fade-in элементов

**Чеклист готовности:**

* \[ \] После регистрации показывается онбординг  
* \[ \] Можно выбрать роль  
* \[ \] Заполнение профиля работает пошагово  
* \[ \] Progress bar обновляется  
* \[ \] После завершения — редирект на dashboard  
* \[ \] Онбординг больше не показывается

**Файлы для работы:**

* `app/onboarding/welcome/page.tsx` — новый  
* `app/onboarding/role/page.tsx` — новый  
* `app/onboarding/profile/page.tsx` — новый  
* `app/onboarding/complete/page.tsx` — новый  
* middleware или layout для проверки `onboarding_completed`

**Тестовый сценарий:**

1. Создать нового юзера  
2. После входа → показывается welcome screen  
3. Выбрать "Я исполнитель"  
4. Заполнить все шаги  
5. Кликнуть "Начать работу"  
6. Попасть на dashboard, больше онбординг не показывается

---

### **📅 ДЕНЬ 11: Портфолио для Workers**

**Главная цель:** Визуальное доверие через фото работ

**Задачи:**

**Создать таблицу `portfolio_items` (30 мин)**

 CREATE TABLE portfolio\_items (  
  id UUID PRIMARY KEY DEFAULT uuid\_generate\_v4(),  
  worker\_id UUID REFERENCES users(id),  
  image\_url TEXT NOT NULL,  
  title VARCHAR(200),  
  description TEXT,  
  category VARCHAR(50),  
  date DATE,  
  created\_at TIMESTAMP DEFAULT NOW()  
);

CREATE INDEX idx\_portfolio\_worker ON portfolio\_items(worker\_id);

1.   
2. **Создать API endpoints (1 час)**

   * `/api/portfolio/upload` — POST (загрузка фото в Supabase Storage)  
   * `/api/portfolio/add` — POST (создание записи)  
   * `/api/portfolio/list` — GET (список работ worker)  
   * `/api/portfolio/delete` — DELETE  
3. **Создать компонент `PortfolioUpload` (2 часа)**

   * Файл: `components/portfolio/PortfolioUpload.tsx`  
   * Drag & drop зона для фото  
   * Или кнопка выбора файла  
   * Preview загруженного фото  
   * Форма:  
     * Название работы  
     * Описание  
     * Категория (select)  
     * Дата выполнения  
   * Кнопка "Добавить в портфолио"  
   * Лимит 10 фото  
4. **Создать компонент `PortfolioGrid` (1 час)**

   * Файл: `components/portfolio/PortfolioGrid.tsx`  
   * Masonry grid (библиотека `react-masonry-css`)  
   * Lightbox для просмотра (библиотека `yet-another-react-lightbox`)  
   * При клике → полноэкранный просмотр  
   * Кнопка удаления (для владельца)  
5. **Интегрировать на страницу профиля worker (1 час)**

   * Файл: `app/(worker)/profile/page.tsx`  
   * Секция "Моё портфолио"  
   * Если нет работ → "Добавьте свои работы"  
   * Кнопка "Добавить работу"  
6. **Показывать портфолио в карточке worker (30 мин)**

   * В поиске исполнителей (для client)  
   * Маленькие превью (2-3 фото)  
   * Кнопка "Смотреть всё портфолио"

**Чеклист готовности:**

* \[ \] Можно загрузить фото работы  
* \[ \] Можно добавить описание  
* \[ \] Портфолио отображается на профиле  
* \[ \] Lightbox работает (полноэкранный просмотр)  
* \[ \] Можно удалить работу  
* \[ \] Лимит 10 фото работает  
* \[ \] Client видит портфолио в карточке worker

**Файлы для работы:**

* `app/api/portfolio/*` — новые  
* `components/portfolio/PortfolioUpload.tsx` — новый  
* `components/portfolio/PortfolioGrid.tsx` — новый  
* `app/(worker)/profile/page.tsx` — обновить

**Установить библиотеки:**

npm install react-masonry-css yet-another-react-lightbox

**Тестовый сценарий:**

1. Открыть свой профиль как worker  
2. Кликнуть "Добавить работу"  
3. Загрузить фото, добавить название и описание  
4. Фото появляется в портфолио  
5. Кликнуть на фото → открывается полноэкранный просмотр  
6. Client ищет workers → видит превью портфолио

---

### **📅 ДЕНЬ 12: Wizard создания смены**

**Главная цель:** Красивая многошаговая форма создания смены

**Задачи:**

1. **Создать компонент `ShiftWizard` (4 часа)**

   * Файл: `components/shifts/ShiftWizard.tsx`  
   * 6 шагов:  
2. **Шаг 1: Категория и название**

   * Select категории специалиста  
   * Input название смены  
   * Textarea описание (опционально)  
3. **Шаг 2: Дата и время**

   * Date picker (дата смены)  
   * Time picker (начало)  
   * Time picker (конец) или duration  
4. **Шаг 3: Локация**

   * Input адрес (с автокомплитом через Yandex Maps API)  
   * Карта (показать точку)  
   * Textarea дополнительная информация (парковка, код домофона)  
5. **Шаг 4: Ставка и количество**

   * Input ставка (₽)  
   * Input количество человек  
   * Checkbox "Инструмент требуется"  
6. **Шаг 5: Требования**

   * Checkbox "Своя спецодежда"  
   * Textarea особые требования  
   * Input минимальный рейтинг (1-5)  
7. **Шаг 6: Контакты и правила**

   * Input контактное лицо

   * Input телефон на площадке

   * Textarea правила площадки / безопасность

   * Progress bar вверху

   * Кнопки "Назад" / "Далее" / "Опубликовать"

   * Validation на каждом шаге

8. **Интеграция Yandex Maps (1 час)**

   * Зарегистрироваться на [developer.tech.yandex.ru](https://developer.tech.yandex.ru/)  
   * Получить API key

Добавить в `.env.local`:  
 NEXT\_PUBLIC\_YANDEX\_MAPS\_API\_KEY=your\_key

*   
  * Использовать `@yandex/ymaps3-types`  
9. **Обновить страницу создания смены (30 мин)**

   * Файл: `app/(client)/shifts/create/page.tsx`  
   * Заменить старую форму на `ShiftWizard`  
10. **Сохранение черновика (30 мин)**

    * При переходе между шагами → сохранять в localStorage  
    * Если не завершил → можно вернуться и продолжить

**Чеклист готовности:**

* \[ \] Wizard отображается со всеми 6 шагами  
* \[ \] Progress bar обновляется  
* \[ \] Можно перемещаться вперёд/назад  
* \[ \] Validation работает на каждом шаге  
* \[ \] Карта показывает выбранный адрес  
* \[ \] После "Опубликовать" → смена создаётся  
* \[ \] Черновик сохраняется в localStorage

**Файлы для работы:**

* `components/shifts/ShiftWizard.tsx` — новый  
* `app/(client)/shifts/create/page.tsx` — обновить  
* `components/maps/YandexMap.tsx` — новый (опционально)

**Тестовый сценарий:**

1. Кликнуть "Создать смену"  
2. Заполнить все 6 шагов  
3. На шаге 3 ввести адрес → карта показывает точку  
4. Кликнуть "Опубликовать"  
5. Смена создаётся, редирект на страницу смены

---

### **📅 ДЕНЬ 13-14: UI/UX Полировка**

**Главная цель:** Довести продукт до премиального уровня

**Задачи День 13:**

1. **Loading states везде (2 часа)**

   * Skeleton loaders для карточек  
   * Spinner для кнопок при submit  
   * Progress bar для загрузки файлов  
   * Shimmer эффект  
2. **Empty states (2 часа)**

   * Компонент: `components/ui/EmptyState.tsx`  
   * Иллюстрации для:  
     * Нет смен  
     * Нет откликов  
     * Нет сообщений  
     * Нет истории платежей  
     * Нет портфолио  
   * Призыв к действию (CTA)  
3. **Error states (1 час)**

   * Компонент: `components/ui/ErrorState.tsx`  
   * Для 404, 500, Network errors  
   * Кнопка "Попробовать снова"  
4. **Toast notifications (1 час)**

   * Установить `react-hot-toast`  
   * Success: "Смена создана ✅"  
   * Error: "Ошибка при сохранении ❌"  
   * Info: "Уведомление отправлено 📨"  
   * Позиция: top-right  
   * Auto-dismiss через 3 сек

**Задачи День 14:**

5. **Микро-анимации (3 часа)**

   * Установить `framer-motion`

Hover эффекты на карточках:  
 \<motion.div whileHover={{ scale: 1.02 }}\>

*   
  * Fade-in при появлении элементов  
  * Slide-in для модалок  
  * Button press эффекты  
6. **Accessibility (2 часа)**

   * Все кнопки ≥44px touch targets  
   * Focus states (outline) на всех интерактивных элементах  
   * ARIA labels где нужно  
   * Keyboard navigation (Tab)  
   * Проверить контраст (WCAG AA)  
7. **Responsive проверка (1 час)**

   * Проверить на:  
     * iPhone 12/13/14 (390px)  
     * iPhone Pro Max (428px)  
     * iPad (768px)  
     * Desktop (1280px+)  
   * Все touch targets комфортны  
   * Модалки не ломаются  
8. **Final polish (1 час)**

   * Проверить все тексты (опечатки)  
   * Единообразие стилей  
   * Иконки везде одинаковые (lucide-react)  
   * Цвета по Design System

**Чеклист готовности:**

* \[ \] Все loading states на месте  
* \[ \] Empty states для всех списков  
* \[ \] Error states обработаны  
* \[ \] Toast notifications работают  
* \[ \] Микро-анимации на ключевых элементах  
* \[ \] Touch targets ≥44px  
* \[ \] Focus states видны  
* \[ \] Всё работает на mobile

**Файлы для работы:**

* `components/ui/EmptyState.tsx` — новый  
* `components/ui/ErrorState.tsx` — новый  
* `components/ui/LoadingSkeleton.tsx` — новый  
* Обновить все существующие компоненты

**Установить библиотеки:**

npm install framer-motion react-hot-toast

**Тестовый сценарий:**

1. Открыть пустую страницу смен → Empty state  
2. Создать смену → Loading → Toast "Создано"  
3. Навести на карточку → Hover эффект  
4. Tab по форме → Focus states видны  
5. Открыть на iPhone → всё работает

---

### **🎯 MILESTONE: КОНЕЦ НЕДЕЛИ 2**

**Проверочный чеклист:**

* \[ \] ✅ Чат 1-на-1 работает в реальном времени  
* \[ \] ✅ Онбординг показывается новым юзерам  
* \[ \] ✅ Worker может добавить портфолио (до 10 фото)  
* \[ \] ✅ Wizard создания смены (6 шагов \+ карта)  
* \[ \] ✅ Все loading / empty / error states  
* \[ \] ✅ Toast notifications  
* \[ \] ✅ Микро-анимации и hover эффекты  
* \[ \] ✅ Accessibility (touch targets, focus, ARIA)  
* \[ \] ✅ Mobile responsive

**Что должно впечатлять:**

1. Онбординг — пользователь понимает, как работает платформа  
2. Портфолио — визуальное доверие  
3. Wizard — создание смены не вызывает вопросов  
4. Чат — коммуникация как в Telegram  
5. Анимации — ощущение премиального продукта

---

## **🗓️ НЕДЕЛЯ 3: ДОВЕРИЕ \+ АДМИНКА**

**Цель недели:** Довести до production-ready — верификация, бейджи, админка, документы

---

### **📅 ДЕНЬ 15-16: Верификация (без Госуслуг)**

**Главная цель:** Система доверия через документы и телефон

**Задачи День 15:**

1. **SMS-верификация телефона (2 часа)**

   * Зарегистрироваться на [sms.ru](https://sms.ru/) или [sms-pilot.ru](https://smspilot.ru/)  
   * Получить API key  
   * Создать endpoint `/api/verify/send-sms`  
   * Сгенерировать 4-значный код  
   * Отправить SMS  
   * Сохранить код в Redis/временную таблицу (5 мин TTL)  
2. **Создать endpoint `/api/verify/confirm-phone` (30 мин)**

   * Принимает код  
   * Проверяет совпадение  
   * Обновляет `users.phone_verified = true`  
3. **Компонент верификации телефона (1 час)**

   * Файл: `components/verification/PhoneVerification.tsx`  
   * Input телефона  
   * Кнопка "Отправить код"  
   * Input 4-значного кода  
   * Кнопка "Подтвердить"  
   * Countdown таймер (можно повторно через 60 сек)  
4. **Загрузка паспорта (2 часа)**

   * Компонент: `components/verification/PassportUpload.tsx`  
   * Кнопка "Загрузить фото паспорта"  
   * Preview загруженного фото  
   * Upload в Supabase Storage (приватный bucket)  
   * Сохранить URL в `worker_profiles.passport_photo_url`  
   * Статус: `passport_status = 'pending'`  
5. **Badge "Верифицирован ✓" (30 мин)**

   * Компонент: `components/user/VerificationBadge.tsx`  
   * Показывать если:  
     * `phone_verified = true`  
     * `passport_status = 'approved'`  
   * Зелёная галочка \+ текст

**Задачи День 16:**

6. **Админ-модерация паспортов (3 часа)**

   * Страница: `app/admin/verification/page.tsx`  
   * Список всех `pending` паспортов  
   * Для каждого:  
     * Фото паспорта  
     * ФИО юзера  
     * Кнопка "Одобрить"  
     * Кнопка "Отклонить" \+ причина  
   * После одобрения:  
     * `passport_status = 'approved'`  
     * Уведомление worker  
7. **Страница верификации для worker (1 час)**

   * `app/(worker)/verification/page.tsx`  
   * Шаг 1: Подтвердить телефон  
   * Шаг 2: Загрузить паспорт  
   * Статусы:  
     * ❌ Не верифицирован  
     * ⏳ На проверке  
     * ✅ Верифицирован  
8. **Интеграция в профиль (30 мин)**

   * Показывать badge на профиле  
   * Показывать badge в карточках в поиске  
   * Фильтр "Только верифицированные" работает

**Чеклист готовности:**

* \[ \] SMS с кодом приходит на телефон  
* \[ \] Можно подтвердить телефон по коду  
* \[ \] Можно загрузить фото паспорта  
* \[ \] Админ видит pending паспорта  
* \[ \] Админ может одобрить/отклонить  
* \[ \] Badge "Верифицирован ✓" показывается  
* \[ \] Фильтр по верифицированным работает

**Файлы для работы:**

* `app/api/verify/send-sms/route.ts` — новый  
* `app/api/verify/confirm-phone/route.ts` — новый  
* `components/verification/PhoneVerification.tsx` — новый  
* `components/verification/PassportUpload.tsx` — новый  
* `components/user/VerificationBadge.tsx` — новый  
* `app/admin/verification/page.tsx` — новый  
* `app/(worker)/verification/page.tsx` — новый

**Тестовый сценарий:**

1. Worker открывает `/verification`  
2. Вводит телефон → получает SMS  
3. Вводит код → телефон подтверждён  
4. Загружает фото паспорта  
5. Админ видит в очереди на модерацию  
6. Админ одобряет  
7. Worker получает уведомление  
8. Badge появляется на профиле

---

### **📅 ДЕНЬ 17: Бейджи и достижения**

**Главная цель:** Геймификация через автоматические бейджи

**Задачи:**

**Создать таблицу `user_badges` (30 мин)**

 CREATE TABLE user\_badges (  
  id UUID PRIMARY KEY DEFAULT uuid\_generate\_v4(),  
  user\_id UUID REFERENCES users(id),  
  badge\_type VARCHAR(50) NOT NULL,  
  earned\_at TIMESTAMP DEFAULT NOW(),  
  UNIQUE(user\_id, badge\_type)  
);

1.   
2. **Определить типы бейджей (30 мин)**

   * Файл: `lib/badges.ts`  
   * Типы:  
     * `verified` — Верифицирован  
     * `reliable_50` — 50+ смен без срывов  
     * `top_rated` — ТОП-10% по рейтингу  
     * `has_tools` — Свой инструмент  
     * `punctual` — Без опозданий (10+ смен)  
     * `experienced` — 100+ смен  
     * `newcomer` — Первая смена

**Создать функцию автоматического расчёта (2 часа)**

 async function calculateUserBadges(userId: string) {  
  const user \= await getUser(userId)  
  const stats \= await getUserStats(userId)  
    
  const badges \= \[\]  
    
  if (user.phone\_verified && user.passport\_status \=== 'approved') {  
    badges.push('verified')  
  }  
    
  if (stats.total\_shifts \>= 50 && stats.cancellation\_rate \< 0.05) {  
    badges.push('reliable\_50')  
  }  
    
  if (user.rating \>= 4.8 && isInTop10Percent(user.id)) {  
    badges.push('top\_rated')  
  }  
    
  if (stats.punctuality\_rate \> 0.95 && stats.total\_shifts \>= 10\) {  
    badges.push('punctual')  
  }  
    
  // ... и т.д.  
    
  await updateUserBadges(userId, badges)  
}

3.   
4. **Trigger пересчёта бейджей (1 час)**

   * После завершения смены  
   * После верификации  
   * Раз в день (cron job через Vercel Cron)  
5. **Компонент отображения бейджей (2 часа)**

   * Файл: `components/user/BadgesList.tsx`  
   * Красивые иконки для каждого типа  
   * Tooltip с описанием при hover  
   * На профиле: горизонтальный список  
   * В карточках: 2-3 главных бейджа  
6. **Страница "Все достижения" (1 час)**

   * `app/(worker)/achievements/page.tsx`  
   * Grid всех возможных бейджей  
   * Заработанные — цветные  
   * Не заработанные — серые \+ условие получения  
   * Progress bars (например, "45/50 смен до бейджа")

**Чеклист готовности:**

* \[ \] Бейджи автоматически рассчитываются  
* \[ \] Показываются на профиле  
* \[ \] Tooltip объясняет условие получения  
* \[ \] Страница достижений работает  
* \[ \] Progress bars показывают прогресс

**Файлы для работы:**

* `lib/badges.ts` — новый  
* `app/api/badges/calculate/route.ts` — новый  
* `components/user/BadgesList.tsx` — новый  
* `components/user/BadgeIcon.tsx` — новый  
* `app/(worker)/achievements/page.tsx` — новый

**Тестовый сценарий:**

1. Worker завершает 50-ю смену без срывов  
2. Автоматически получает бейдж "50+ смен"  
3. Бейдж появляется на профиле  
4. Hover → tooltip "50+ смен без срывов"  
5. Открыть `/achievements` → видны все бейджи

---

### **📅 ДЕНЬ 18-19: Админ-панель**

**Главная цель:** Управление платформой

**Задачи День 18:**

1. **Создать layout для админки (30 мин)**

   * Файл: `app/admin/layout.tsx`  
   * Проверка роли: `if (user.role !== 'admin') redirect('/')`  
   * Боковое меню:  
     * 👥 Пользователи  
     * 📋 Смены  
     * 📊 Статистика  
     * 💳 Финансы  
     * ✅ Верификация  
2. **Страница управления пользователями (3 часа)**

   * Файл: `app/admin/users/page.tsx`  
   * Таблица всех юзеров:  
     * ID, ФИО, роль, телефон, рейтинг, дата регистрации  
   * Фильтры:  
     * По роли (worker/client/shef)  
     * По статусу (active/banned)  
     * По верификации  
   * Поиск по имени/телефону  
   * Действия:  
     * Просмотр профиля  
     * Бан / Разбан  
     * Редактировать  
3. **Модалка бана пользователя (1 час)**

   * Компонент: `components/admin/BanUserModal.tsx`  
   * Выбор причины (select):  
     * Срыв 3+ смен  
     * Конфликтное поведение  
     * Мошенничество  
     * Фальшивые документы  
   * Textarea дополнительное описание  
   * Date picker срок бана (опционально)  
   * Кнопка "Заблокировать"  
4. **Детальная страница юзера для админа (2 часа)**

   * `app/admin/users/[id]/page.tsx`  
   * Вся информация:  
     * Профиль  
     * Статистика смен  
     * История платежей  
     * Все рейтинги (полученные/данные)  
     * Логи действий  
   * Кнопки: Бан, Редактировать, Удалить

**Задачи День 19:**

5. **Страница управления сменами (2 часа)**

   * `app/admin/shifts/page.tsx`  
   * Таблица всех смен:  
     * ID, название, заказчик, статус, дата, сумма  
   * Фильтры по статусу  
   * Поиск по названию  
   * Действия:  
     * Просмотр  
     * Отменить (в крайних случаях)  
6. **Страница базовой статистики (3 часа)**

   * `app/admin/stats/page.tsx`  
   * Карточки метрик:  
     * Всего пользователей (по ролям)  
     * Смен создано / завершено / отменено  
     * Оборот (₽)  
     * Средний чек смены  
   * Графики:  
     * Регистрации по дням (line chart)  
     * Смены по категориям (bar chart)  
     * Выручка по неделям (area chart)  
   * Период: за всё время / месяц / неделю  
7. **Страница финансов (2 часа)**

   * `app/admin/finance/page.tsx`  
   * Метрики:  
     * Общая выручка  
     * Комиссии платформы  
     * Выплаты исполнителям  
     * Pending платежи  
   * Таблица последних транзакций  
   * Кнопка "Экспорт отчёта" (CSV)

**Чеклист готовности:**

* \[ \] Админ-панель доступна только admin-роли  
* \[ \] Таблица пользователей с фильтрами  
* \[ \] Можно забанить/разбанить юзера  
* \[ \] Детальная страница юзера  
* \[ \] Таблица смен  
* \[ \] Статистика с графиками  
* \[ \] Финансовый отчёт

**Файлы для работы:**

* `app/admin/layout.tsx` — новый  
* `app/admin/users/page.tsx` — новый  
* `app/admin/users/[id]/page.tsx` — новый  
* `app/admin/shifts/page.tsx` — новый  
* `app/admin/stats/page.tsx` — новый  
* `app/admin/finance/page.tsx` — новый  
* `components/admin/BanUserModal.tsx` — новый

**Тестовый сценарий:**

1. Зайти как admin  
2. Открыть `/admin/users`  
3. Найти проблемного юзера  
4. Кликнуть "Забанить" → выбрать причину  
5. Юзер заблокирован  
6. Открыть `/admin/stats`  
7. Графики отображают данные

---

### **📅 ДЕНЬ 20: PDF Документы (Акты)**

**Главная цель:** Автогенерация юридических актов

**Задачи:**

1. **Выбрать библиотеку (15 мин)**

   * Вариант 1: `pdf-lib` (клиентский, простой)  
   * Вариант 2: `puppeteer` (серверный, красивый)  
   * **Рекомендация:** `pdf-lib` для MVP

**Установить библиотеку (5 мин)**

 npm install pdf-lib

2.   
3. **Создать шаблон акта (1 час)**

   * Файл: `lib/documents/act-template.ts`  
   * Структура акта:  
     * Заголовок: "АКТ ВЫПОЛНЕННЫХ РАБОТ №{id}"  
     * Дата  
     * Заказчик: название, ИНН, адрес  
     * Исполнитель: ФИО, ИНН/СЗ  
     * Название работ (смена)  
     * Дата выполнения  
     * Сумма  
     * Подписи (текст)

**Создать функцию генерации (2 часа)**

 async function generateAct(shiftId: string) {  
  const shift \= await getShift(shiftId)  
  const client \= await getUser(shift.client\_id)  
  const worker \= await getUser(shift.worker\_id)  
  const payment \= await getPayment(shift.id)  
    
  const pdfDoc \= await PDFDocument.create()  
  const page \= pdfDoc.addPage()  
    
  // Рисуем текст  
  page.drawText('АКТ ВЫПОЛНЕННЫХ РАБОТ', { x: 50, y: 750 })  
  // ... и т.д.  
    
  const pdfBytes \= await pdfDoc.save()  
    
  // Сохранить в Supabase Storage  
  const url \= await uploadToStorage(pdfBytes, \`acts/${shiftId}.pdf\`)  
    
  return url  
}

4.   
5. **Создать endpoint `/api/documents/generate-act` (1 час)**

   * Принимает `shift_id`  
   * Генерирует PDF  
   * Сохраняет в Storage  
   * Сохраняет запись в таблицу `documents`  
   * Возвращает URL  
6. **Кнопка "Скачать акт" (30 мин)**

   * На странице завершённой смены  
   * Для обеих сторон (client \+ worker)  
   * При клике → генерация (если ещё нет) → скачивание  
7. **Страница истории документов (2 часа)**

   * `app/(shared)/documents/page.tsx`  
   * Таблица документов:  
     * Тип (Акт)  
     * Смена  
     * Дата  
     * Кнопка "Скачать"  
   * Фильтр по дате  
   * Кнопка "Скачать все (ZIP)" — опционально

**Чеклист готовности:**

* \[ \] Можно сгенерировать акт для завершённой смены  
* \[ \] PDF содержит все данные (заказчик, исполнитель, сумма)  
* \[ \] Кнопка "Скачать акт" работает  
* \[ \] Страница истории документов  
* \[ \] Документы сохраняются в Storage

**Файлы для работы:**

* `lib/documents/act-template.ts` — новый  
* `lib/documents/generate-act.ts` — новый  
* `app/api/documents/generate-act/route.ts` — новый  
* `app/(shared)/documents/page.tsx` — новый

**Тестовый сценарий:**

1. Завершить смену  
2. Оплатить  
3. Кликнуть "Скачать акт"  
4. PDF скачивается  
5. Открыть — все данные корректны  
6. Открыть `/documents` → акт там есть

---

### **📅 ДЕНЬ 21: ФИНАЛЬНОЕ ТЕСТИРОВАНИЕ \+ ПОЛИРОВКА**

**Главная цель:** Протестировать весь флоу, зафиксить баги, задеплоить

**Задачи:**

1. **End-to-End тестирование (3 часа)**

   * Сценарий 1: Worker  
     * Регистрация → онбординг → верификация  
     * Поиск смен → отклик  
     * Получение одобрения → check-in  
     * Завершение → оценка  
     * Просмотр статистики  
   * Сценарий 2: Client  
     * Регистрация → онбординг  
     * Создание смены (wizard)  
     * Просмотр откликов → одобрение  
     * Мониторинг check-in  
     * Завершение → оплата через ЮКасса  
     * Скачивание акта  
   * Сценарий 3: Чат  
     * Client пишет worker  
     * Worker отвечает в реальном времени  
     * Отправка фото  
   * Проверить на устройствах:  
     * iPhone Safari  
     * Android Chrome  
     * Desktop Chrome  
2. **Фикс критичных багов (2 часа)**

   * Составить список найденных проблем  
   * Приоритизировать (критичные → мелкие)  
   * Пофиксить критичные  
3. **Performance optimization (1 час)**

   * Проверить Lighthouse score  
   * Оптимизировать изображения (WebP, lazy loading)  
   * Code splitting (dynamic imports)  
   * Цель: Performance \>80  
4. **SEO \+ мета-теги (30 мин)**

   * Обновить `metadata` в layout  
   * Open Graph теги  
   * Favicon  
5. **Подготовка demo-аккаунтов (30 мин)**

   * Создать 3 тестовых аккаунта:  
     * demo-worker@shef-montazh.ru  
     * demo-client@shef-montazh.ru  
     * demo-admin@shef-montazh.ru  
   * Заполнить реалистичными данными  
   * Создать несколько смен в разных статусах  
6. **Deploy на production домен (30 мин)**

   * Купить домен (если ещё нет)  
   * Настроить в Vercel  
   * Проверить env variables  
   * Deploy  
7. **Smoke test production (30 мин)**

   * Проверить все основные флоу на проде  
   * Убедиться что ЮКасса работает  
   * Telegram notifications приходят

**Чеклист готовности:**

* \[ \] Весь флоу worker работает без багов  
* \[ \] Весь флоу client работает без багов  
* \[ \] Чат в реальном времени  
* \[ \] ЮКасса принимает платежи  
* \[ \] PDF акты генерируются  
* \[ \] Push-уведомления приходят  
* \[ \] Всё работает на mobile  
* \[ \] Performance \>80 в Lighthouse  
* \[ \] Production deploy успешен  
* \[ \] Demo-аккаунты готовы

**Файлы для работы:**

* Всё (финальные проверки)  
* `next.config.js` — оптимизация  
* `app/layout.tsx` — metadata

**Тестовый сценарий:**

1. Пройти полный флоу как worker  
2. Пройти полный флоу как client  
3. Протестировать чат  
4. Провести тестовый платёж  
5. Проверить на 3 устройствах  
6. Открыть на production домене  
7. Убедиться что всё работает

---

### **🎯 MILESTONE: КОНЕЦ НЕДЕЛИ 3**

**Финальный чеклист:**

**✅ Верификация и доверие:**

* \[ \] SMS-верификация телефона  
* \[ \] Загрузка паспорта  
* \[ \] Админ-модерация документов  
* \[ \] Badge "Верифицирован ✓"

**✅ Бейджи:**

* \[ \] Автоматический расчёт достижений  
* \[ \] 7+ типов бейджей  
* \[ \] Страница достижений с прогрессом

**✅ Админ-панель:**

* \[ \] Управление пользователями (бан/разбан)  
* \[ \] Модерация смен  
* \[ \] Статистика с графиками  
* \[ \] Финансовые отчёты

**✅ Документы:**

* \[ \] PDF акты после завершения  
* \[ \] История документов  
* \[ \] Скачивание

**✅ Production:**

* \[ \] Deploy на домен  
* \[ \] Demo-аккаунты  
* \[ \] Всё протестировано  
* \[ \] Performance оптимизирован

---

## **🎉 РЕЗУЛЬТАТ ПОСЛЕ 3 НЕДЕЛЬ**

### **✅ У вас будет:**

**Полнофункциональная платформа:**

* 👥 Регистрация \+ онбординг \+ верификация  
* 🔍 Поиск смен / workers с фильтрами  
* 📝 Wizard создания смены (6 шагов)  
* ✅ Check-in с фото \+ геолокация  
* ⭐ Взаимные рейтинги  
* 💬 Чат в реальном времени  
* 💳 Оплата через ЮКасса  
* 📄 PDF акты  
* 📊 Статистика \+ графики  
* 🎯 Бейджи и достижения  
* ⚡ Push-уведомления (7 типов)  
* 👨‍💼 Админ-панель  
* 📱 Mobile-first responsive

**Технологии:**

* Next.js 14 \+ App Router  
* TypeScript  
* Supabase (Auth, DB, Storage, Realtime)  
* ЮКасса (платежи)  
* Telegram Bot API (уведомления)  
* Yandex Maps (геолокация)  
* Recharts (графики)  
* Framer Motion (анимации)  
* SMS.ru (верификация)

**Production-ready:**

* Deployed на домен  
* SSL сертификат  
* Мониторинг (Sentry)  
* Performance оптимизирован  
* SEO настроен  
* Demo-аккаунты

---

## **📋 ЕЖЕДНЕВНЫЙ WORKFLOW**

**Каждое утро:**

1. Открыть этот файл (`07_ACTION_PLAN_3WEEKS.md`)  
2. Найти текущий день  
3. Прочитать задачи  
4. Начать с первой задачи

**Каждый вечер:**

1. Отметить ✅ выполненные задачи в чеклисте  
2. Закоммитить код  
3. Краткий тест на мобильном  
4. Записать проблемы (если есть)

**Каждое воскресенье:**

1. Milestone review  
2. Показать прогресс кому-то  
3. Собрать feedback  
4. Спланировать следующую неделю

---

## **🔧 ПОЛЕЗНЫЕ КОМАНДЫ**

**Разработка:**

npm run dev              \# Запуск dev-сервера  
npm run build            \# Сборка production  
npm run lint             \# Проверка кода  
npm run type-check       \# TypeScript проверка

**Git:**

git add .  
git commit \-m "feat: день X \- \[название задачи\]"  
git push

**Vercel:**

vercel                   \# Deploy preview  
vercel \--prod            \# Deploy production

---

## **📚 ССЫЛКИ**

**Документация:**

* Next.js: https://nextjs.org/docs  
* Supabase: https://supabase.com/docs  
* ЮКасса: https://yookassa.ru/developers  
* Telegram Bot API: https://core.telegram.org/bots/api

**Инструменты:**

* v0.dev (для генерации компонентов)  
* Claude Code (для помощи с кодом)  
* Figma / Excalidraw (для визуализации)

**Мониторинг:**

* Vercel Analytics  
* Sentry (ошибки)

---

## **🎯 ПОМНИ:**

1. **Не перфекционизм** — 80% готовности достаточно, доделаешь потом  
2. **Один день \= одна большая фича** — фокус  
3. **Тестируй на mobile** — каждый день  
4. **Commit каждый день** — не теряй прогресс  
5. **Показывай другим** — feedback бесценен

---

**🚀 ПОЕХАЛИ\! ЧЕРЕЗ 21 ДЕНЬ У ТЕБЯ БУДЕТ ПРОДУКТ-КЕЙС, КОТОРЫЙ ВПЕЧАТЛИТ ЛЮБОГО\!**

Удачи\! 💪 EOF

Теперь копирую в outputs и предоставляю тебе:

\`\`\`bash  
cp /home/claude/07\_ACTION\_PLAN\_3WEEKS.md /mnt/user-data/outputs/

