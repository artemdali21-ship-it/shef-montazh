# 🚀 Миграция: Машина состояний для смен

## ⚠️ ВАЖНО: Прочитайте перед запуском!

Эта миграция добавляет полную машину состояний для управления жизненным циклом смен, откликов и работников.

---

## 📋 Что изменится

### Новые статусы для смен:
- **draft** — Черновик
- **published** — Опубликовано
- **applications** — Есть отклики
- **shortlist** — Шортлист
- **offer_sent** — Отправлено приглашение
- **accepted** — Принято
- **check_in** — Выход на объект
- **in_progress** — В работе
- **completed** — Завершено
- **payout** — Оплачено
- **review** — Оценено
- **cancelled** — Отменено

### Новые статусы для откликов:
- **pending** — Ожидает
- **shortlisted** — В шортлисте
- **invited** — Приглашён
- **accepted** — Принял
- **declined** — Отклонён
- **cancelled** — Отменён

### Новые статусы для работников:
- **assigned** — Назначен
- **on_way** — В пути
- **checked_in** — На месте
- **completed** — Завершил

---

## 🔄 Маппинг старых статусов

### Таблица shifts:
```
open        → published
in_progress → in_progress
completed   → completed
cancelled   → cancelled
другие      → draft
```

### Таблица applications:
```
pending  → pending
accepted → accepted
rejected → declined
другие   → pending
```

---

## 🛠️ Как запустить миграцию

### Шаг 1: Создать бэкап базы данных

**Обязательно!** Создайте полный бэкап перед запуском:

```bash
# Для локальной базы данных
pg_dump -U postgres -d shef_montazh > backup_$(date +%Y%m%d_%H%M%S).sql

# Для Supabase (через Dashboard)
# Settings → Database → Backups → Create Backup
```

### Шаг 2: Запустить миграцию

**Вариант A: Через Supabase Dashboard**

1. Откройте Supabase Dashboard
2. Перейдите в SQL Editor
3. Скопируйте содержимое файла `add_status_enums.sql`
4. Вставьте в редактор
5. Нажмите "Run"

**Вариант B: Через psql**

```bash
psql -U postgres -d shef_montazh -f migrations/add_status_enums.sql
```

**Вариант C: Через Supabase CLI**

```bash
supabase db push
```

### Шаг 3: Проверить результат

После миграции выполните проверочный запрос:

```sql
-- Проверить статусы смен
SELECT status, COUNT(*) FROM shifts GROUP BY status;

-- Проверить статусы откликов
SELECT status, COUNT(*) FROM applications GROUP BY status;

-- Проверить статусы работников
SELECT worker_status, COUNT(*) FROM shift_workers GROUP BY worker_status;
```

---

## 🚨 Если что-то пошло не так

### Откат миграции

В файле `add_status_enums.sql` в конце есть секция с откатом (закомментирована).

**⚠️ ИСПОЛЬЗОВАТЬ ТОЛЬКО В КРАЙНЕМ СЛУЧАЕ!**

Раскомментируйте и выполните SQL код из секции "ОТКАТ МИГРАЦИИ".

### Восстановление из бэкапа

```bash
# Для локальной базы данных
psql -U postgres -d shef_montazh < backup_YYYYMMDD_HHMMSS.sql

# Для Supabase
# Settings → Database → Backups → Restore
```

---

## ✅ После успешной миграции

1. **Проверьте UI:**
   - Откройте панель заказчика
   - Проверьте отображение статусов (должны быть красивые badges)
   - Попробуйте изменить статус смены

2. **Проверьте API:**
   - Создайте новую смену (статус должен быть `draft`)
   - Опубликуйте смену (статус → `published`)
   - Проверьте автопереходы

3. **Проверьте логи:**
   - Убедитесь что нет ошибок в консоли браузера
   - Проверьте логи сервера

---

## 📚 Дополнительная информация

### Файлы, которые были созданы:

- `lib/types/status.ts` — TypeScript типы и labels
- `components/status/StatusBadge.tsx` — Компонент для отображения статусов
- `lib/status/transitions.ts` — Логика переходов между статусами
- `lib/api/shifts.ts` — API функции для управления статусами
- `migrations/add_status_enums.sql` — SQL миграция

### Документация:

- [Диаграмма состояний](../docs/state-machine.md) — _(создать отдельно)_
- [API Reference](../docs/api.md) — _(создать отдельно)_

---

## 🐛 Troubleshooting

### Ошибка: "type shift_status already exists"

Миграция уже была выполнена. Пропустите создание типов.

### Ошибка: "column status is of type text"

Нужно сначала удалить ограничения на колонку:

```sql
ALTER TABLE shifts ALTER COLUMN status DROP DEFAULT;
ALTER TABLE shifts ALTER COLUMN status TYPE VARCHAR(50);
-- Затем запустите миграцию
```

### Ошибка: "invalid input value for enum"

Есть статусы в БД, которые не входят в маппинг. Проверьте:

```sql
SELECT DISTINCT status FROM shifts WHERE status NOT IN ('open', 'in_progress', 'completed', 'cancelled');
```

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи
2. Убедитесь что бэкап создан
3. Откатите миграцию при необходимости
4. Обратитесь к разработчику

---

**Удачи! 🚀**
