# 🔧 Решение проблем с кешем

## ❗ Проблема: Изменения не видны после обновления кода

Если вы видите что исправления не работают, хотя код обновлен:

---

## 🚀 БЫСТРОЕ РЕШЕНИЕ (рекомендуется):

```bash
# Запустите скрипт очистки кеша
./clear-cache.sh

# Затем запустите dev server заново
npm run dev
```

---

## 📝 ПОШАГОВОЕ РЕШЕНИЕ:

### Шаг 1: Остановить dev server

В терминале где запущен `npm run dev`:
- Нажмите `Ctrl + C` (или `Cmd + C` на Mac)

### Шаг 2: Очистить кеши Next.js

```bash
# Удалить .next папку
rm -rf .next

# Удалить кеш node_modules
rm -rf node_modules/.cache

# Очистить npm кеш
npm cache clean --force
```

### Шаг 3: Перезапустить dev server

```bash
npm run dev
```

### Шаг 4: Очистить кеш браузера

**В Chrome/Edge:**
- Нажмите `Ctrl + Shift + R` (Windows/Linux)
- Или `Cmd + Shift + R` (Mac)
- Это "жёсткая перезагрузка" без кеша

**В Safari:**
- Нажмите `Cmd + Option + R`

**В Firefox:**
- Нажмите `Ctrl + Shift + R` (Windows/Linux)
- Или `Cmd + Shift + R` (Mac)

**На мобильном устройстве:**

**iPhone/iPad (Safari):**
1. Настройки → Safari
2. Очистить историю и данные
3. Подтвердить
4. Перезапустить Safari
5. Открыть сайт заново

**Android (Chrome):**
1. Меню (⋮) → Настройки
2. Конфиденциальность и безопасность
3. Очистить данные браузера
4. Выбрать "Кешированные изображения и файлы"
5. Очистить
6. Перезапустить Chrome

---

## 🔍 ПРОВЕРКА: Работают ли исправления?

### 1. Enter навигация в регистрации:

**Ожидаемое поведение:**
- Введите номер телефона
- Нажмите Enter
- ✅ Должно переключиться на поле Email

**Если не работает:**
- Откройте DevTools (F12)
- Проверьте Console на ошибки
- Убедитесь что файл `RegistrationScreen.tsx` обновлен

### 2. Валидация чекбокса:

**Ожидаемое поведение:**
- НЕ отмечайте чекбокс "Согласен с условиями"
- Нажмите Enter на последнем поле или кнопку "Продолжить"
- ✅ Должна появиться ошибка: "Необходимо согласиться с условиями"

**Если не работает:**
- Проверьте что валидация вызывается
- Откройте DevTools → Console
- Проверьте что `acceptedTerms` = false

### 3. Скролл на мобилке:

**Ожидаемое поведение:**
- Откройте профиль на мобильном
- ✅ Страница должна скроллиться вниз
- Нажмите на смену
- ✅ Детали смены должны скроллиться

**Если не работает:**
- Проверьте в DevTools → Elements
- Найдите главный div профиля
- Убедитесь что есть:
  ```css
  height: 100dvh;
  overflow: hidden;
  ```
- И внутренний div имеет:
  ```css
  flex: 1;
  overflow-y: auto;
  min-height: 0;
  -webkit-overflow-scrolling: touch;
  ```

---

## 🐛 Дополнительная диагностика:

### Проверить какие файлы изменены:

```bash
git status
```

### Проверить последние коммиты:

```bash
git log --oneline -5
```

### Убедиться что вы на правильной ветке:

```bash
git branch
```

### Посмотреть изменения в файле:

```bash
git diff components/RegistrationScreen.tsx
```

---

## 📱 Специально для мобильной версии:

### Если используете Telegram Mini App:

1. Закройте приложение полностью
2. Очистите кеш Telegram:
   - iOS: Настройки → Данные и память → Очистить кеш
   - Android: Настройки → Данные и память → Использование памяти → Очистить кеш
3. Перезапустите Telegram
4. Откройте Mini App заново

### Если тестируете через браузер на телефоне:

1. Откройте настройки браузера
2. Найдите "Очистить кеш" или "Очистить данные сайта"
3. Выберите только кеш (НЕ удаляйте cookies если не нужно)
4. Очистите
5. Закройте браузер полностью (из списка приложений)
6. Откройте заново

---

## 🆘 Если ничего не помогает:

### Вариант 1: Полная переустановка зависимостей

```bash
rm -rf node_modules
rm -rf .next
npm install
npm run dev
```

### Вариант 2: Проверить версию Node.js

```bash
node --version
# Должно быть >= 18.17.0

npm --version
# Должно быть >= 9.0.0
```

### Вариант 3: Откатиться на нужный коммит

```bash
# Посмотреть историю
git log --oneline -10

# Откатиться на коммит с исправлениями
git checkout d38097f

# Или создать новую ветку
git checkout -b fix-test d38097f
```

---

## ✅ Контрольный список:

- [ ] Dev server перезапущен
- [ ] `.next` папка удалена
- [ ] Кеш браузера очищен (Ctrl+Shift+R)
- [ ] Страница полностью обновлена
- [ ] DevTools Console не показывает ошибок
- [ ] Проверил на актуальной ветке (`main`)
- [ ] Убедился что последний коммит - `d38097f` или новее

---

**Если проблема все еще есть:**

1. Сделайте скриншот Console (F12 → Console)
2. Сделайте скриншот Elements (F12 → Elements → найдите проблемный элемент)
3. Скопируйте результат `git log --oneline -3`
4. Отправьте всё разработчику

---

Удачи! 🚀
